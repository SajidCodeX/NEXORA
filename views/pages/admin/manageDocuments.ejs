<link rel="stylesheet" href="/css/admin/manageDocuments.css">
<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Upload Date</th>
      <th>University</th>
      <th>Status</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% files.forEach(file => { %>
      <tr>
        <td><%= file.userId.email %></td>
        <td><%= new Date(file.uploadDate).toLocaleDateString() %></td>
        <td><%= file.university %></td>
        <td><%= file.status %></td>
        <td>
          <a href="/admin/view-parsed/<%= file._id %>">View</a>
          <a <a href="/admin/download/<%= file._id %>" class="btn download-btn">Download</a>
          <a href="/admin/delete-file/<%= file._id %>">Delete</a>
        </td>
      </tr>
    <% }); %>
  </tbody>
</table>  
